__pycache__/
*.pyc
.venv/
venv/

# Local generated data / logs -- don't commit real or test run data
factory_audit_log.csv
hermis_data.db

# Trained model artifacts -- large binary files, don't belong in git history
# (host these separately -- e.g. a release asset, or retrain on demand)
*.keras
*.pkl
*.h5

# Streamlit
.streamlit/secrets.toml

# OS
.DS_Store
